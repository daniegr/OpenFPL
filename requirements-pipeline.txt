# Dependencies for the automatic data pipeline (fpl_engine/).
# The OpenFPL inference models themselves are pinned in plug.txt.
#
# Storage is stdlib sqlite3 (no server, no extra dependency) — exactly the
# "simple local SQLite" store requested.
pandas>=2.0
numpy>=1.24
requests>=2.28      # optional; urllib fallback exists if absent
PyYAML>=6.0         # scoring rules
joblib>=1.3         # load OpenFPL model artefacts
scikit-learn>=1.5   # scalers bundled with OpenFPL
xgboost>=3.0        # OpenFPL ensemble members
pytest>=7.0         # tests
